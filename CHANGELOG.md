# 변경 이력 (Changelog)

이 저장소(`claude-code-template`)의 버전별 변경 기록이다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 느슨히 따르고 [유의적 버전](https://semver.org/lang/ko/)을 사용한다. 변경의 단일 진실의 원천은 `VERSION`과 `HANDBOOK.md`이며, 이 파일은 그 요약·색인이다.

> 버전 정책: 큰 변경(모델·가격·effort·새 에이전트)은 **minor**, 호환성이 깨지면 **major**. 핸드북 자체에 변경이 있으면 템플릿 버전도 함께 올린다.

---

## [Unreleased]

- (현재 비어 있음 — 다음 변경은 여기 누적)

---

## [v0.6.0] — 2026-07-27 — Claude Opus 5 반영

**Claude Opus 5**를 반영한 네 번째 모델 유지관리 사이클(T1). `model: opus` 별칭이 자동 승격되므로 **에이전트 파일은 한 줄도 바뀌지 않았고**, 4.8과 **가격·토크나이저가 동일**해 비용 계산과 토큰 예산도 그대로다. 이번 사이클의 실질은 모델명 치환이 아니라 **모델의 기본 행동 성향 변화에 맞춘 운영 지침 신설**이다 — Opus 5는 자체 검증을 알아서 하고, 서브에이전트 위임을 4.8보다 **과하게** 하며(4.8은 반대로 과소했다), 응답·산출물이 길고 작업 범위를 넓힌다. 아울러 **전 문서 정합성 감사를 처음 수행**해 누적된 stale 사실 6건을 정정했다.

### Changed — 바뀜
- **Opus 티어 이전**: `model: opus` 별칭 → **Opus 5**(`claude-opus-5`). architect·deep-debugger·pr-reviewer가 파일 수정 없이 자동 승격. Opus 4.8은 "직전 플래그십"으로 재배치. **단 별칭 승격에는 Claude Code v2.1.219 이상이 필요**하고, **제공자별 해석이 다르다**(Microsoft Foundry의 `opus`는 아직 Opus 4.6) — §3.1에 메모 신설.
- **§3.2 토크나이저 절 재구성**: family별 두 갈래 서술 → **세대 기준 단일 규칙**(4.7 이후 = 새 토크나이저)로 정리하고 표로 만듦. Opus 4.8 → Opus 5는 **같은 세대**라 재계량 부담이 작다고 서술하되, "세대가 같다"는 공식 서술이지 "토크나이저가 동일하다"는 보증은 아니라는 단서를 병기. 재계량이 필수였던 Sonnet 5 사이클과 대비시켜 "T1이라고 항상 토크나이저 세대가 바뀌지는 않는다"는 교훈을 남김.
- **§4.1 effort 권장 시작점이 역방향으로 이동**: 4.7·4.8의 "코딩·에이전틱이면 **xhigh**에서 시작"이 Opus 5에서는 **"기본값 high에서 시작"**으로 바뀌었다(공식 확인). low·medium을 비용·지연의 주 레버로 적극 쓰라는 권고와, **"이전 모델 effort 설정을 재사용하지 말고 스윕을 다시 돌리라"**는 지침을 함께 편입.
- **§5.4 Fable 에스컬레이션 전제 갱신**: Opus 5가 **같은 가격에 천장을 올렸으므로** 에스컬레이션 임계가 위로 이동. "Opus 4.8로는 안 됐으니 Fable"이라는 과거 판단은 Opus 5에서 다시 시험해야 한다는 단서 추가.

### Added — 추가
- **운영 지침 신설 — Opus 5 행동 변화 대응**(이번 사이클의 핵심):
  - **§9.2 위임 상한**: Opus 5는 위임을 과하게 하므로 마스터 CLAUDE.md에 넣을 상한 문구와, **결정론적 상한 세 가지**(깊이 기본 3 `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` / 동시 실행 기본 20 `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS` / 세션 총량, 둘 다 v2.1.217+)를 함께 제시. 방향이 4.8과 **반대**임을 명시(4.8은 위임 과소). 같은 문구를 두 프로필의 `CLAUDE.md`에도 실제로 반영했다.
  - **§9.3 단서**: 범위는 좁히되 **"완료 기준"에 검증 지시는 넣지 않는다**.
  - **§12.9 과잉 검증**: 검증 지시는 고쳐 쓰지 말고 **삭제**가 정답. 일반적 프롬프팅 통념과 어긋나므로 프롬프트 라이브러리에 예외를 두라는 주의 포함.
  - **§12.10 장황함**: effort로는 가시 응답 길이가 줄지 않으므로 프롬프트로 조절. 문서 산출물 길이·자기 정정 서술 억제 문구 포함.
  - **§12.11 범위 확장**: 범위 고정 문구와 "결과물 개수 세기" 점검법.
- **§4.4에 Opus 5 thinking 계약**: **적응형 추론 기본 ON**(4.8은 파라미터 생략 시 OFF), `max_tokens`가 thinking+응답을 함께 덮으므로 절단 주의, **`thinking:{disabled}`는 effort `high` 이하에서만 허용**(xhigh·max는 400).
- **§4.3·§4.5 Claude Code 동작 메모**: Opus 5에는 모델 기본 effort **hold가 없어** 이전에 설정한 레벨이 그대로 이어진다. `ultrathink`만 인식되는 키워드이며 "think hard" 등은 평문이다.
- **§11.4 캐시 최소 프리픽스 표 신설**: 모델별 값(Opus 5·Fable 5 **512** / Opus 4.8·Sonnet 5·4.6 1,024 / Opus 4.7 2,048 / Opus 4.6·Haiku 4.5 4,096)과 "짧으면 에러 없이 조용히 캐시 안 됨" 경고. **Opus 5는 문턱이 4.8의 절반**이라 이전에 포기한 프롬프트를 재검토할 값어치가 있음.
- **§11.5 배치 300K 출력**: `output-300k-2026-03-24` 베타로 배치 출력 상한 상향(동기 API의 128K와 별개).
- **부록 B 체크리스트 4항목**: 명세 외 파일 생성 여부, effort 필드 분포, 위임 과다, 산출물 길이.
- **부록 D 용어집에 `refusal` 항목** 추가, xhigh·적응형 추론·1M·Fast mode·Dynamic workflows 항목 갱신.

### Fixed — 정정 (전체 정합성 감사 결과)
변경분이 아니라 **이전 사이클들이 남긴 부채**다. 감사를 돌리지 않았다면 잡히지 않았을 항목들이다.
- **캐시 최소 토큰의 자기모순**: §5.4는 "Fable 2048토큰(Opus 4.8의 4096보다 낮음)", §11.4는 "4.8은 1,024토큰"으로 **같은 문서가 서로 다른 값**을 말하고 있었다. 공식 값은 **Fable 5·Opus 5 = 512, Opus 4.8 = 1,024**로, 두 서술 다 틀렸다.
- **§1.3 토크나이저 서술이 §3.2와 불일치**: 갱신된 세대 규칙·30% 수치를 반영하지 못하고 옛 "1.35×/1.3×" 표현이 남아 있었다.
- **§11.6 fast mode 지원 범위 stale**: "Opus 4.8이 제공"에 머물러 있었다. 실제로는 **Opus 5·4.8 지원, 4.7은 에러, 4.6은 무시(표준가 청구), Claude API 1st-party 전용**이다.
- **공식 인용문 변경**: 토크나이저 인플레이션이 공식 문서에서 "up to 35%" → **"approximately 30%"**로 바뀌어 인용과 본문 수치를 함께 갱신.
- **존재하지 않는 절 참조**: HANDBOOK 머리말의 `§3.6 정책`은 핸드북에 없는 절이었다(사용자 외부 정책 지칭) → "README 버전 정책 참조"로 교체.
- **시점 스탬프 stale**: §4.5 제목 "(2026년 5월 현재 — 이제 공식 지원)", §11.6 제목 "(2026년 5월 신규 레버)" → 시점 표기 제거.

### Added — 감사가 발견한 신기능 편입
공식 sub-agents 문서 전수 대조에서 핸드북이 모르던 **결정론적 안전장치**들이 드러났다.
- **§12.6에 `maxTurns`**: deep-debugger 폭주 방지를 프롬프트 문장에만 의존하지 않고 frontmatter 필드로 강제하는 선택지.
- **§14.5 메모리 절 전면 보강**: 스코프가 `project` 하나가 아니라 **`user`/`project`/`local` 세 가지**이며 각각 저장 경로가 다르다. 함정 둘을 신설 — ① **메모리를 켜면 Read·Write·Edit이 자동 활성화**되므로 **읽기 전용 에이전트(architect·pr-reviewer)에는 주지 않는다**(§12.4 설계와 충돌), ② **auto memory가 꺼져 있으면 필드가 조용히 무효**.
- **§4.5 `model` 필드 값 정정**: 별칭 `sonnet`·`opus`·`haiku`·**`fable`**·전체 ID·`inherit`을 받으며, 지원되지 않는 effort 레벨은 "무시"가 아니라 **지원되는 최고 레벨로 클램프**된다.

### Fixed — 에이전트 리뷰 반영 (2차)

커밋 후 **관점을 분리한 리뷰어 3인**(사실 검증 / 정합성 재감사 / 편집)을 붙여 나온 정정이다. 리뷰 지시에 심각도 필터를 걸지 않고 "찾은 것을 확신도·심각도와 함께 전부 보고"하게 한 뒤 걸러냈다 — 이번 사이클에 §12.7로 문서화한 방식을 리뷰 자체에 적용한 것이다.

- **🔴 §12.8 "서브에이전트는 자식 서브에이전트를 만들 수 없다" — 거짓이었다.** 공식 문서는 "기본 3계층까지 자식을 스폰할 수 있다"고 명시한다(v2.1.172부터 기본 동작). 이 항목의 논거였던 "무한 중첩을 막는 안전장치"가 성립하지 않는다. **수평 협업(A↔B) 불가는 여전히 사실**이므로 그 부분은 남기고, 수직 중첩은 설정으로 관리하는 값임을 표로 정리했다(깊이/동시/총량 세 상한).
  - **배포 산출물 3곳까지 전파**: `profiles/{standard,lite}/CLAUDE.md`·`shared/claude-md-common.md`가 같은 거짓을 담고 있었다. 이 파일들은 사용자가 프로젝트로 복사해 가는 산출물이고, `sync.sh`의 **의도적 제외 목록**에 있어 drift 검사에도 걸리지 않았다 — SSOT만 고치면 끝이 아니라는 사례로 기록한다.
- **§12.7 분류기 동작 보강**: 첫 요청의 워크스페이스 컨텍스트(CLAUDE.md·git 상태)만으로 분류기가 발화할 수 있다는 점(`claude --safe-mode`로 원인 분리)과 `/config`로 자동 전환을 끄는 법.
- **`(API에선 "extra")` 삭제** (§4.1·부록 D): 공식 문서 어디에도 없는 표기였다. API 값은 `xhigh` 그대로다.
- **Opus 4.7의 xhigh 기본값에 한정어 추가** (§3.1·§4.1): xhigh는 **Claude Code** 기본값이고 API 기본값은 high다.
- **`§3.6` 유령 참조 완전 제거**: HANDBOOK만 고치고 `docs/maintenance-guide.md:87`에 남아 있었다 — 정정을 기록한 파일 자체에 결함이 남은 사례.
- **SWE-bench Pro 수치에 기준 모델 명시** (§5.4): "80.3% vs 69.2%"의 69.2%는 **Opus 4.8** 수치인데 무라벨이라, Opus 5가 천장을 올렸다고 논하는 인접 문단과 겹쳐 Opus 5 수치로 오독될 수 있었다.
- **effort 클램프 규칙 표현 정밀화** (§4.5): "지원되는 최고 레벨로 내려서 실행"은 **"설정한 값 이하에서 지원되는 최고 레벨"**의 축약이었다. 축약 때문에 "4.6이 max를 지원하는데 왜 xhigh가 high가 되나"라는 모순으로 읽혔다.
- **§12.8의 `Agent` 도구 조언에 현황 명시**: 이 템플릿의 8개 에이전트는 이미 `tools`를 명시적으로 나열해 `Agent`가 없으므로 스폰이 불가하다 — 조치가 필요한 것은 `tools`를 생략한 새 에이전트를 만들 때다.
- **라우팅 스킬 명세를 설계에 맞춤** (§7.2·부록 A·부트스트랩 §6): 명세가 "한국어·영어 키워드 모두 수록"을 요구했으나 실제 스킬·부록 C는 한국어만 싣는다. **영어 트리거는 각 에이전트의 `description`이 담당**하는 것이 이 템플릿의 설계이므로, 스킬에 중복 수록하도록 요구하던 명세 문장 쪽을 고쳤다.
- **`inherit` 비용 표현 정정** (두 라우팅 스킬): "비용 4배"는 이 저장소의 가격표에서 도출되지 않는 수치였다 → "Sonnet 대비 약 1.7배, Haiku 대비 5배".
- **잔여 stale 표기**: `maintenance-guide` 절차의 "Opus 4.X"·"Opus 4.7 권장값" 예시, 용어집 Task budget 항목의 Opus 5 누락, §10.4의 "§5 수동 라우팅" 참조(§5·§7이 맞음).

### Fixed — 에이전트 리뷰 반영 (3차·편집 관점)

편집 리뷰어의 총평이 이번 라운드의 기조를 정했다 — **"사실은 맞게 고쳤지만 필요한 것의 두 배를 썼다."** 이 PR이 스스로 진단한 Opus 5의 성향(§12.10 장황·§12.11 범위 확장)이 **수정 과정에서 그대로 재현됐다.** 그래서 3차는 추가가 아니라 **삭감**이 목표였고, 부록 B.0 신설을 포함해도 순 분량이 줄었다.

**모순·오류 (머지 차단급)**
- **위임 상한 문구가 §10 워크플로우와 충돌했다** (§9.2·양 프로필 `CLAUDE.md`·`shared/claude-md-common.md`): "자기 작업을 검증·재확인하는 용도로 서브에이전트를 쓰지 않는다"가 **항상 로드되는 CLAUDE.md**에 들어가면서, implementer 결과를 pr-reviewer가 보는 §10.1·§10.4 워크플로우를 금지하는 것으로 읽혔다. **"이미 끝낸 작업을 같은 관점에서 재확인"**으로 좁히고 "다른 권한·다른 관점의 리뷰는 해당하지 않는다"를 명시했다. 2차의 프로필 전파 수정이 이 충돌을 오히려 키운 사례다.
- **§12.9의 적용 범위가 없었다**: "검증 지시를 삭제하라"가 모델 무관 규칙으로 읽혀, Sonnet·Haiku 에이전트 본문의 자가 점검까지 지우게 만들 수 있었다. **Opus 5로 도는 것(마스터 + Opus 에이전트 3개)에만 해당**함을 명시하고, **무엇이 "검증 지시"인가의 경계**를 추가했다 — 부록 A 부트스트랩 §9의 품질 체크리스트는 "다시 봐라"가 아니라 **완료 기준의 명세**이므로 삭제 대상이 아니다.
- **§4.4의 잘못된 절 참조**: "thinking을 끄면 부작용" 근거로 §12.9를 가리켰으나 §12.9는 검증 지시 절이다. 동시에 **부록 E에는 근거가 있는데 본문에 대응 주장이 없던** 사실(도구 호출 평문 누출·내부 태그 노출)을 §4.4 본문에 인라인했다.
- **`shared/claude-md-common.md`에 위임 상한 문구 누락**: 두 프로필 `CLAUDE.md`에는 들어갔으나 공통 참조본에는 빠져 있었다. **sync 비대상 파일이라 `--check`로는 잡히지 않는** 드리프트가 또 나왔다(2차의 §12.8 사례와 같은 구조).
- **"가격 불변 = 비용 불변"이 과한 주장이었다** (§5.1·README): 단가는 같지만 **thinking 기본 ON·길어진 산출물·늘어난 위임**이 모두 같은 작업에 더 많은 토큰을 쓰게 한다. "단가가 같다고 청구서가 같지는 않다 — 실제 사용량은 다시 관찰한다"로 교체.

**중복·범위 삭감 (정보 손실 없음)**
- **pr-reviewer의 Opus 5 메모가 4곳에 중복**(§5.4·§6.4·§12.7·부록 D) → **§6.4를 정본**으로, §5.4·§12.7은 포인터로 축약.
- **§12.7의 분류기 blockquote 둘째 문단 삭제**: 생물학 범주 폴백 비대칭·`/model` 복귀·버전 요건은 소프트웨어 개발 핸드북의 범위 밖이다. 이 절에서 실제로 쓸모 있는 것(첫 요청부터 발화·`safe-mode` 진단·`/config` 토글)만 한 문단으로 남기고, **부록 E의 근거 줄에서도 삭제한 주장을 함께 뺐다** — 근거만 남기면 위에서 잡은 것과 똑같은 결함이 된다.
- **§9.2 ②가 "§12.8의 표 참조"라 써 놓고 표 내용을 복제**하고 있었다 → 포인터 한 문장으로.
- **§4.3 blockquote가 바로 위 불릿과 같은 말**("스윕을 다시 돌리라")을 반복 → Opus 5 고유 사실(effort hold 부재)만 남김.
- **§9.2의 "두 번째 줄" 오프바이원**: 위 충돌 수정으로 줄이 밀렸는데 순번 참조가 그대로였다 → 내용으로 참조하도록 변경.

**구조·보강**
- **§12.8 제목 정합**: 제목은 "서브에이전트끼리 협업 기대"(수평)인데 본문 70%가 수직 중첩이었다. **"서브에이전트의 협업·중첩을 오해하기"**로 바꾸고 수평/수직을 각각 한 문단으로 정리했다. 2차에서 붙인 "정정(v0.6.0)" blockquote는 본문에 흡수.
- **부록 B.0 "Opus 5로 올라올 때 (기존 사용자)" 신설**: 기존 셋업 사용자가 직접 해야 할 일(CLI 최소 버전·effort 재확인·검증 지시 삭제·위임 상한·길이 지시·frontmatter 무수정)이 §3·§4·§9·§12에 흩어져 있고 한 자리에 없었다. **이번 사이클은 새 사용자보다 기존 사용자의 할 일이 더 많은데** 정작 그들을 위한 진입점이 없던 셈이다.
- **§13.2 이상 신호 2줄 보강**: "무엇을 관찰할 것인가"를 담당하는 절이 정작 이번 사이클의 주제(행동 변화)에 대해 비어 있었다. "마스터가 사소한 일까지 위임한다 → §9.2"를 신설하고, 기존 "결과가 장황하다"에 **"effort를 낮추는 것으로는 잡히지 않는다"**(§12.10)를 병기.

### Unchanged — 의도적으로 그대로 둔 것
- **가격·토크나이저**: Opus 5 = $5/$25(4.8과 동일), 배치 $2.50/$12.50, 토크나이저 세대 동일 → §5.1 매트릭스의 비용 판단·§11 레버 계산 전부 무변경.
- **과거 버전 CHANGELOG 항목**: v0.2.0의 "최대 35%", v0.5.0의 "Fable 캐시 2048토큰" 등은 **그 시점의 기록이므로 수정하지 않는다.** 정정 사실은 이 v0.6.0 항목이 담당한다 — 변경 이력을 소급 수정하면 이력의 의미가 사라진다.
- **8개 에이전트 default**: `model`·`effort` frontmatter 무수정(architect·deep-debugger high, pr-reviewer xhigh). Opus 5의 공식 권장 시작점이 high로 내려오면서 **오히려 권장값과 더 잘 맞게 됐다**. `shared/` 무변경이라 `sync.sh` 전파 불필요.
- **pr-reviewer 모델 고정 안 함**: Opus 5의 사이버 세이프가드 강화로 방어적 보안 감사도 `refusal`이 날 수 있으나(cyber 범주 권장 폴백은 Opus 4.8), **default는 `model: opus` 유지**하고 **§6.4를 정본으로** 메모만 남겼다(§5.4·§12.7은 포인터). 오탐은 특정 용어에 국한된 것으로 보고되며, default를 이전 세대에 묶는 비용이 더 크다고 판단.
- **에이전트 시스템 프롬프트 본문**: 행동 변화 대응은 **핸드북 문서 레이어와 마스터 CLAUDE.md에만** 반영. 8개 에이전트 각각의 프롬프트 본문 재조정(검증 지시 삭제·위임 상한 내재화)은 실사용 관찰 후 다음 사이클로.
- **README.en.md**: **버전·가격·버전별 모델명** 미포함 정책으로 무변경(per-cycle churn 0). 정책 문구를 정밀화한다 — 영어판은 티어명(Opus/Sonnet/Haiku)은 쓰되 **버전 번호가 붙은 모델명**은 쓰지 않는다. 티어명은 모델 사이클마다 바뀌지 않으므로 churn 0이라는 목적에 어긋나지 않는다.

### Notes — 근거 (공식/관찰 구분)
- 공식 확인: Opus 5의 $5/$25·1M(기본=최대)/128K·기본 effort high·전 effort 레벨·적응형 추론 기본 ON·4.8과 동일 토크나이저·신뢰 지식 컷오프 2026-05·캐시 최소 512토큰·**Priority Tier 미지원**(+ 신규 용량 약정 구매 종료)·fast mode $10/$50(Claude API 전용)·배치 $2.50/$12.50·`thinking:{disabled}`의 effort 상한·"effort는 가시 응답 길이를 줄이지 않는다"·행동 특성 6종·`opus` 별칭 v2.1.219+·제공자별 별칭 해석·effort hold 부재·`ultrathink` 키워드·sub-agents frontmatter 필드 일체.
- 이번 사이클에는 **실사용 관찰 기반 서술을 새로 추가하지 않았다** — 모든 신규 서술이 공식 문서 근거다. 기존 Fable 5 관련 관찰(보안 용어 오탐·응답 지연)은 "관찰" 표기를 유지.

### Maintenance — 유지관리
- `VERSION` v0.5.0 → v0.6.0. 7개 버전 스탬프 정합(VERSION·HANDBOOK 첫 줄·README·부트스트랩 ×2·CLAUDE.md ×2). `scripts/sync.sh --check` → OK(`shared/` 무변경).
- Opus 에이전트 3개(`architect`·`deep-debugger`·`pr-reviewer`)는 **본문 한 줄씩만** 수정했다("Opus 4.8+에서 적용" → "Opus 4.8 이후 적용" 표현 정리) — frontmatter의 `model`·`effort`는 무수정이고, 세 파일 모두 `shared/`에 없는 standard 전용 파일이라 sync 전파 대상이 아니다.
- 2차 리뷰에서 **`profiles/{standard,lite}/CLAUDE.md`·`shared/claude-md-common.md`·두 라우팅 SKILL**을 추가로 수정했다(§12.8 거짓 주장 제거 + 위임 상한 문구 + `inherit` 비용 표현). 이 다섯 파일은 모두 `sync.sh`의 의도적 제외 대상이라 **`--check`가 통과해도 내용 드리프트는 잡히지 않는다** — 다음 사이클의 감사 자동화 후보에 이 경로를 포함한다.
- 마스터 모델 권장을 `profiles/standard/CLAUDE.md`에서 Opus 4.8 → **Opus 5**로 갱신.
- 부록 A ↔ `docs/bootstrap-prompts/standard.md` 본문 byte-identical 재확인, 부트스트랩 §5 명세 ↔ 8개 에이전트 실제 frontmatter 전수 대조 일치.
- `docs/maintenance-guide.md`: **모델 사이클 표본 n=2 → n=3**. §0에 "별칭 승격의 전제(최소 CLI 버전·제공자별 해석)" 메모 신설. §3 T1 행에 "HANDBOOK §9·§12(행동 변화 동반 시)" 추가. **§4에 절차 5-2 "전체 문서 정합성 감사(B1~B6)"를 상설 단계로 신설**. §7에 사이클 4 결과 추가 — 새 관찰 두 가지("T1이 가격·토크나이저를 그대로 두고 행동만 바꿀 수 있다", "권장값이 역방향으로 바뀔 수 있다")와 감사 자동화(`scripts/audit.sh`) 후보를 기록.

---

## [v0.5.0] — 2026-07-22 — Fable 5 재검토 반영 (의사결정 가이드 재도입)

v0.3.0에서 접근 제한으로 철회했던 **Fable 5** 가이드를 재검토해 재도입한 사이클 — v0.4.0 Notes의 "별도 사이클에서 접근성·비용·'Opus 위 티어' 배치를 재검토한다" 예고를 회수한다. 미국 수출통제(2026-06-12)로 중단됐던 접근이 06-30 해제·07-01 재배포를 거쳐 **07-20 Max·Team Premium 플랜 정식 편입**(주간 한도의 50%, Pro는 크레딧+$100 1회)으로 안정됐고, 재배포 후 실사용 데이터가 쌓여 근거 기반 재검토가 가능해졌다. 핵심 결론: **8개 에이전트 default 무변경** — Fable 5는 §5.4의 수동 에스컬레이션 전용으로 편입하되, 마스터의 "세션 시작 시 선택"은 개방한다.

### Added — 추가
- **핸드북 §5.4 재도입·전면 갱신**: "상위 능력이 필요할 때 — Opus effort↑ vs Fable 5". 철회 전(PR #4) 버전 대비 갱신점 — ① **`fable` 별칭 공식 지원**(Claude Code v2.1.170+, 철회 전 "별칭 없음"은 stale 정정), ② **Max·Team Premium 정식 편입** 메모(비용 계산은 표준 API $10/$50 기준 유지 + 플랜 관점 병기 — 구독 사용자는 달러 비용 대신 한도 소모가 실질 비용), ③ **금지선 갱신** — 보안 리뷰(pr-reviewer) 금지 신설(안전 분류기가 방어적 보안 감사까지 오탐 + refusal 시 Opus 4.8 자동 폴백 → 한도만 더 쓰고 사실상 Opus 결과), 마스터는 "도중 교체 금지 유지·**시작 시 선택 개방**"으로 완화(Fable 이득이 장기 자율·병렬 오케스트레이션에 집중 + 분업 구조가 2× 소모와 상보적), ④ **Fable 고유 제약 요약**(thinking 상시 on·prefill 불가·30일 보존·fast mode 미지원·캐시 최소 2048토큰·과잉 처방 프롬프트 지양).
- **모델 레이어 편입**: §3.1 모델 표 Fable 행($10/$50, 기본추천 Opus 4.8 유지 명시), §3.2(Opus 4.8과 동일 토크나이저 — 이동 시 토큰 재계량 불필요), §3.3(1M 목록), §4.1(effort 표 5행 + Fable 메모 — 전 레벨 지원·기본 high·thinking 파라미터 생략), §4.4(적응형 추론 상시 on 계약), §5.1 매트릭스 메모("어떤 행도 Fable로 치환 안 됨"), 부록 D 용어집(Fable 5·Mythos 5), 부록 E 출처(공식 문서 5종 + 근거 3줄, 공식/관찰 구분).
- **부트스트랩 §2에 Fable 비배정 명시** (standard·lite ×2 + 부록 A 동기화) — 부트스트랩 산출물이 Fable을 에이전트에 배정하지 않도록 가드.

### Unchanged — 의도적으로 그대로 둔 것
- **8개 에이전트 default·3원칙·아키텍처 골격**: architect·deep-debugger·pr-reviewer = Opus 4.8, Sonnet·Haiku 티어 불변, `model`·`effort` frontmatter 무수정. 근거 — Fable의 이득은 길고 복잡한 자율 작업에 집중되고(SWE-bench Pro 80.3% vs 69.2%, 공식) 격리된 단일 호출(서브에이전트)은 격차가 작은 영역인데(관찰) 단가는 2×. pr-reviewer는 위 분류기 오탐·자동 폴백 때문에 승격 실익이 음수.
- **§12.1**(마스터 세션 도중 `/model` 교체 금지 — 캐시 보호)과 **§8.2** 부트스트랩 권장(Opus 4.8/high).
- **README.en.md**: 버전·가격·모델명 미포함 정책으로 무변경(per-cycle churn 0).

### Notes — 근거 (공식/관찰 구분)
- 공식 확인: $10/$50·1M/128K·Opus 4.8과 동일 토크나이저·effort 전 레벨(기본 high)·thinking 상시 on·`fable` 별칭·refusal 시 Opus 4.8 자동 폴백(설정 가능)·SWE-bench Pro 80.3% vs 69.2%·07-20 플랜 편입.
- 실사용 관찰(커뮤니티, 2026-07): 짧고 스코프 명확한 작업에서 격차 축소, 응답 60초~수분(vs Opus 3~15초), 인가된 방어적 보안 감사·SSH/iptables/syscall 용어 오탐 보고. 관찰은 본문에 "관찰"로 표기해 공식 사실과 구분.

### Maintenance — 유지관리
- `VERSION` v0.4.0 → v0.5.0. 7개 버전 스탬프 정합(VERSION·HANDBOOK 첫 줄·README·부트스트랩 ×2·CLAUDE.md ×2). `scripts/sync.sh --check` → OK(`shared/` 무변경 — default 불변이므로 에이전트 원본 불변).
- `docs/maintenance-guide.md`: §1 아래 **"새 티어 메모" 재도입·확장**(기존 family 위 새 티어 = 경량 추가 원칙 + "접근 정책 유동기엔 [Unreleased], 안정 후 minor 승격" 교훈), §7에 **사이클 3 결과** 추가(철회-재도입 왕복이 "default 무변경·단독 bump 없음" 정책의 회수 가능성을 실증, 재도입 시 이전 문서 사실 명제 전수 재검증 교훈).
- **리뷰 반영**: 부록 A §2 라인의 v0.4.0 유래 드리프트(Sonnet 괄호구 누락) 정합 — 부록 A와 `docs/bootstrap-prompts/standard.md`의 해당 라인이 다시 byte-identical. `best` 별칭에 공식 근거 라벨 부여, §5.4의 §2.3 인용을 §8.2 단독으로 정밀화.

---

## [v0.4.0] — 2026-07-01 — Claude Sonnet 5 반영

2026-06-30 출시된 **Claude Sonnet 5**를 반영한 두 번째 모델 유지관리 사이클(T1). 아키텍처와 **Opus 티어는 그대로 두고**, Sonnet 티어의 모델·가격·토크나이저·effort 레이어만 갱신했다. `model: sonnet` 별칭이 Claude Code 기본값으로 Sonnet 5를 가리키므로 **에이전트 파일은 한 줄도 바뀌지 않았다**(family alias 자동 승격). 핵심은 모델명 치환이 아니라 **새 토크나이저(~30%)·적응형 추론 기본 ON·xhigh 지원 편입 + stale 사실 정정**이다.

### Changed — 바뀜
- **Sonnet 티어 이전**: `model: sonnet` 별칭 → **Sonnet 5**(Claude Code 기본값). implementer·refactorer·test-writer·doc-writer가 파일 수정 없이 자동 승격. Sonnet 4.6은 "직전 Sonnet"으로 재배치. 배정 티어(3원칙)는 불변.
- **가격**: Sonnet 5 표준 **$3/$15**(4.6과 동일), 2026-08-31까지 **인트로 $2/$10**. 핸드북 §1.1·§3.1·§5·§11.5·부록 A와 부트스트랩 가격 줄 갱신.
- **§3.2 토크나이저 일반화**: "새 토크나이저 = Opus 4.7+ 전용"에서 **"Opus 4.7 이후 + Sonnet 5"**로 확장. Sonnet 5는 4.6 대비 **~30% 토큰 증가**(공식 확인), Sonnet 4.6·Haiku 4.5는 구 토크나이저(무인플레). drop-in 교체지만 토큰 재계량·`max_tokens` 절단 주의 명시.

### Added — 추가
- **effort 표(§4.1)에 Sonnet 5**: low/medium/high/xhigh/max 지원 — **xhigh 신규 획득**(4.6은 미지원). 기본 effort **high**(공식 확인).
- **적응형 추론(§4.4)에 Sonnet 5 편입**: Sonnet 5는 적응형 추론 **기본 ON**(4.6은 OFF), 수동 extended thinking 제거(400), 비기본 sampling 파라미터 400, Priority Tier 미지원 — Opus 4.8과 같은 계약.
- **출처(부록 E)**: Sonnet 5 공식 문서 3종(What's new·소개·마이그레이션 가이드)과 토크나이저·effort 근거 줄 추가. 용어집(xhigh·적응형 추론·1M)에 Sonnet 5 반영.

### Unchanged — 의도적으로 그대로 둔 것
- **아키텍처 골격·Opus 티어**: architect·deep-debugger·pr-reviewer = Opus 4.8 유지(비가역·보안·최고난도 추론은 Anthropic도 Opus 우위로 명시). 마스터 Opus 4.8/high 유지, 8개 역할 분담·3원칙·위임 4요소 불변.
- **에이전트 frontmatter**: `model` 별칭·Opus 3개의 `effort` 필드 무수정. effort 필드는 여전히 **Opus 에이전트에만** 부여(정책 유지 — Sonnet 에이전트는 기본값 사용).
- **범위**: 전략 가이드 보강(Sonnet 마스터 비용 레버·에스컬레이션 기준·Opus 호출 비율 지표)과 Sonnet 에이전트 effort 필드 도입은 이번 사이클 제외.

### Fixed — 정정
- **§4.5 stale 사실**: "Sonnet·Haiku 에이전트에서는 effort가 무시된다"는 Sonnet 5부터 오류 → "Sonnet 5는 effort를 지원하나 본 템플릿은 기본값(적응형 추론 ON) 사용"으로 정정(정책은 유지, 사실만 수정).

### Notes — 관찰 (미채택)
- **Fable 5**: 공식 모델 개요에 `claude-fable-5`가 GA로 노출됨($10/$50, "가장 유능한 널리 공개된 모델"). v0.3.0에서 접근 제한으로 철회했던 상태와 상충하나, 이번 Sonnet 5 사이클 범위 밖 — 별도 사이클에서 접근성·비용·"Opus 위 티어" 배치를 재검토한다.

### Maintenance — 유지관리
- `VERSION` v0.3.0 → v0.4.0. 7개 버전 스탬프 정합(VERSION·HANDBOOK 첫 줄·README·부트스트랩 ×2·CLAUDE.md ×2). `scripts/sync.sh --check` → OK(`shared/` 무변경 — 별칭만 쓰므로 에이전트 원본 불변).
- `docs/maintenance-guide.md`: **모델 사이클 표본 n=1 → n=2**로 갱신. §7에 "사이클 2 결과" 추가 — cadence "4주 이내" 2회 연속 준수 기록, "같은-family T1이 토크나이저·thinking 기본값 변화를 동반할 수 있다"는 새 관찰과 T1 체크리스트 항목 권고.
- `README.en.md`: 버전·가격·모델명 미포함 정책으로 **무변경**(per-cycle churn 0).

---

## [v0.3.0] — 2026-06-14 — 토큰 절감 레퍼런스 검토 반영 + 영어 진입점

외부 토큰 절감 레퍼런스 10종을 검토해 **0-인프라로 채택 가능한 원리만** 핸드북에 흡수한 콘텐츠 사이클(트리거 T6/T7). 모델·effort·아키텍처는 v0.2.0(Opus 4.8) 그대로다.

### Added — 추가
- **토큰 절감 레버 2종 (핸드북 §11)**: ① §11.7 '도구·명령 출력 컴팩션' — 시끄러운 bash/test/build/툴 출력을 마스터 컨텍스트에 넣기 전 필터·추출(§11.3이 *서브에이전트* 산출물을 다룬다면 이 레버는 *도구* 산출물). ② §11.8 '컨텍스트 위생' — 자동 로드 표면 최소화·무거운 산출물 참조화·완료 작업 아카이브(§11.4 캐싱과 상보). "비용 레버 여섯 가지" → "여덟 가지"로, §11 TOC에 잔존하던 "다섯 가지" 표기도 정정.
- **§14.3에 토큰 절감 MCP 후보 명시**: 대형 코드베이스에서 §11.7·§11.8을 자동화할 외부 MCP 후보(Code Review Graph, Context Mode, Claude Context)를 출처·의존성·비용 주의와 함께 추가. 환경 내장 LSP·ast_grep 우선 확인 권고 병기. 템플릿 무번들 원칙은 유지(도구는 프로젝트가 선택적으로 마운트).
- **영어 진입점 (i18n)**: `README.en.md`(한 화면 분량 영어 개요)를 추가하고 `README.md` 상단에 `[English] | 한국어` 전환 링크를 둠. 한국어 문서가 canonical로 유지되며, 영어판에는 버전·가격·모델명을 넣지 않아 per-cycle churn 0. (v0.2.0 사이클에 작성, 이번 릴리즈에 동봉.)

### Removed — 제거
- **Fable 5 의사결정 가이드 (철회)**: `[Unreleased]` 초안에 경량 추가했던 Fable 5(`claude-fable-5`, Opus 위 티어) 의사결정 가이드를 철회. 2026-06 미국 정부의 접근 제한으로 사용 불가가 되어 정식 반영 전 원복 — `HANDBOOK.md` §3.1·§4.1·§5.1·§5.4 및 `docs/maintenance-guide.md` T1 메모를 pre-Fable 상태로. 정식 릴리즈에 포함된 적은 없으며, 철회 사실만 기록으로 남긴다.

### Notes — 검토했으나 미채택 (근거)
외부 토큰 절감 레퍼런스 10종 검토 결과, 다음은 의도적으로 반영하지 않았다:
- **token-optimizer-mcp** (ooples): 메커니즘·벤치마크 문서가 부재해 95% 절감 주장을 검증 불가 — 보류.
- **외부 바이너리·프록시 동봉(RTK 등) / 벡터 검색 인프라(Claude Context의 Milvus+임베딩)**: 복사 기반·무번들 정체성과 충돌. 원리는 §11.7에, 도구는 §14.3 '선택적 후보'로만 반영.
- **Caveman 풀 'caveman voice'**: 출력 토큰은 줄지만 한국어 전문 문서 톤·마스터의 종합 책무(§9.4)와 충돌. 간결성 *원리*는 이미 §11.3·§9.4에 존재.
- **CLAUDE.md 간결성 규칙 세트**(claude-token-efficient 등): 이번 사이클 보류 — CLAUDE.md는 캐시 민감(§11.4)이고 핵심 규칙이 이미 존재. 추후 1회성 안정 추가로 재검토 가능.
- **이미 충분히 커버됨**: Token Savior(심볼 탐색) = 환경 LSP/ast_grep, Token Optimizer(델타 재독·스켈레톤) = §11.2 explorer-first.

### Maintenance — 유지관리
- `VERSION` v0.2.0 → v0.3.0. 7개 버전 스탬프 정합 확인. `scripts/sync.sh --check` → OK(`shared/` 무변경).
- `docs/maintenance-guide.md`: 콘텐츠 사이클(T6/T7)이라 모델 cadence 표본은 1회로 유지 — §2·§7의 "v0.3.0에서 재조정" 예고를 "다음 모델 사이클"로 정정(cadence 재조정은 다음 T1에서).

---

## [v0.2.0] — 2026-05-30 — Claude Opus 4.8 반영

2026-05-28 출시된 **Claude Opus 4.8**을 반영한 첫 유지관리 사이클. 아키텍처는 그대로 두고 **모델·effort 배정 레이어**만 갱신하고 4.8 신기능을 편입했다. 모델명 일괄 치환이 아니라 **effort 기본값 하향 + 신규 비용 레버 편입**이 핵심이다.

### Changed — 바뀜
- **모델 이전**: 마스터와 Opus 에이전트(`architect`·`deep-debugger`·`pr-reviewer`)를 Opus 4.7 → **Opus 4.8**로. 표준 가격은 불변($5 / $25 per MTok). 4.7은 "직전 플래그십"으로 재배치.
- **effort 기본값 하향**: Opus 4.8 기본 effort가 **high**로 내려옴(4.7은 xhigh). 4.8은 같은 effort로 더 좋은 결과를 내므로 가성비 매트릭스·부트스트랩 권장 effort를 한 단계 낮춤(예: 아키텍처 xhigh→high). 부트스트랩 세션 권장: "Opus 4.7 / xhigh" → **"Opus 4.8 / high"** (어려운 부트스트랩만 xhigh).
- **토크나이저**: Opus 4.8은 4.7과 **동일 토크나이저**(같은 텍스트 최대 35% 토큰 증가)를 쓴다 — 문서 표기를 "Opus 4.7 이후(4.8 포함)"로 정리.

### Added — 추가
- **서브에이전트 `effort` frontmatter 필드 채택**: 공식 지원으로 확인되어 Opus 에이전트에 명시 — `architect`·`deep-debugger` = `high`, `pr-reviewer` = `xhigh`. Opus 4.8+에서만 적용되고 Sonnet/Haiku 에이전트에는 부여하지 않음. 구버전·폴백 대비로 본문 시스템 프롬프트 백스톱 지시도 병행.
- **신규 비용/지연 레버**: 핸드북 11장에 **fast mode**($10 / $50, 2.5× 속도; 이전 세대 대비 3× 저렴) 추가 → "비용 레버 다섯 가지"가 "여섯 가지"로. **dynamic workflows / `ultracode`** 분기를 §10.4 대형 리팩토링에 편입(수동 8-에이전트 라우팅의 보완, 대체 아님). **대화 도중 `system` 항목**과 **1,024토큰 캐시 최소값**을 캐싱 절(§11.4)에 반영.
- **배치 가격 갱신**: Opus 4.8 배치 **$2.50 / $12.50**(공식 확인).
- **관찰 갱신**: "low effort 4.8 ≈ max effort 4.7"(SWE-Bench Pro, 시스템 카드 Fig 8.2.A). 정직성 향상(코드 결함을 그냥 넘기는 비율 4.7 대비 약 4× 감소), 툴 트리거 개선.
- **변경 이력 체계**: 이 `CHANGELOG.md` 신설 + README 상단 "최근 업데이트" 콜아웃.

### Unchanged — 의도적으로 그대로 둔 것
- 아키텍처 골격: 마스터 오케스트레이션, 8개 역할 분담, 격리된 컨텍스트, 캐시 보호(마스터 `/model` 교체 금지), Haiku 우선 탐색, 명시적 `model` 필드, 위임 4요소 템플릿.
- Sonnet·Haiku 배정과 3원칙(비가역=Opus / 일상=Sonnet / 대량=Haiku). "Opus 4.8/low가 Sonnet/high보다 싸면서 낫다"는 행은 없음(4.8은 $5/$25 + 토크나이저 인플레이션, Sonnet은 $3/$15).

### Maintenance — 유지관리
- `VERSION` v0.1.0 → v0.2.0. 7개 버전 스탬프 정합 확인. `scripts/sync.sh --check` → OK(`shared/` 무변경).
- `docs/maintenance-guide.md`를 사이클-1 학습으로 보완: 영향 파일 매핑 빈틈(README·`effort` 필드) 반영, "변경 이력 갱신" 절차 단계 추가, worked example 추가.

> 자세한 근거·맥락: `HANDBOOK.md` §3(모델·가격), §4(effort), §5(가성비 매트릭스), §11(비용 레버).

---

## [v0.1.0] — 2026-04 — 최초 릴리즈

- Claude Opus 4.7 기준 서브에이전트 오케스트레이션 템플릿 초판.
- 구성: 핸드북(5부 + 5개 부록), `lite`(3 에이전트)·`standard`(8 에이전트) 프로필, 부트스트랩 프롬프트, 라우팅 스킬, 유지관리 가이드, `scripts/sync.sh`.
